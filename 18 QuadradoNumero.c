#include <stdio.h>
#include <stdlib.h>

int main(){
    int base,resultado;
    base = 100000;
    resultado = base * base;
    printf("O quadrado de %d eh %d\n", base, resultado);
    return 0;
    
}